package list.controller;

import java.io.IOException;
import java.util.List;

import javax.servlet.RequestDispatcher;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import list.service.IShowReviewService;
import list.service.ShowReviewService;
import review.vo.ReviewVO;

@WebServlet("/showReview.do")
public class ShowReviewController extends HttpServlet {

	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		
		IShowReviewService service = ShowReviewService.getInstance();
		List<ReviewVO> reviewList = service.showReviewList();
		
		request.setAttribute("reviewList", reviewList);

		RequestDispatcher disp = request.getRequestDispatcher("/detail.jsp");
		disp.forward(request, response);
		
	}

	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		doGet(request, response);
	}

}
